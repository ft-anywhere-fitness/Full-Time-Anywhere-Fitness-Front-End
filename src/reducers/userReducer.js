// Initial State
const initialState = {
  "username": "",
  "email": "",
  "role_id": null,
  "auth": ""
};

// User Reducer
const userReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default userReducer;
