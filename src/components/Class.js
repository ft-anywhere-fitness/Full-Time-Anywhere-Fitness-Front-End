// Libraries
import React from "react";

const Class = ({ name, type, startTime, duration, intensityLevel, location, currentlyEnrolled, classSizeLimit }) => {
  return (
    <div>
      <h3>Welcome to {name}</h3>
    </div>
  );
};

export default Class;
