const initialState = {
  username: "",
  password: "",
  role: "",
};

const usersReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default usersReducer;
